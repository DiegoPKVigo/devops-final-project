apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev
namePrefix: dev-

# Replace 'bases' with 'resources'
resources:
  - ../../base
  - ./artifacts
  - ./nginx-test
  - ./postgres
  - ./pgadmin
  - ./sonar

  

labels:
  - pairs:
      environment: dev
